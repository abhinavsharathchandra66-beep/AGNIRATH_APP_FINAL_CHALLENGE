# AGNIRATH_APP_CE25B031


the design architecture, of my code is of four parts first is data pipeline second is physcis third is optimiser and last is the main now data pipeline gives values for the irradaince and the slopes from the api then the physics part would calculate the all power factors such as drag, garvity etc then we find SOC values from them by fibnding solar power  we use the optimiser alorithms such as slsql for to reach zeersut then might use grredy or slsql for to maximize the loops then all combine at the main and then we plot the graph at the dashboard and by that i plot the SOC , velocity, acclerartion and power i divided into for parts to debug or find error easily and it looks very deatiled without any clumsy 
the problems i faced and facing while writing code afre by api error which can be removed  by using pandas and then making it nan and inertpolate and then the erroe might be gone and also the power spikes are cominmg huge in graph due to this only i tried but the error isnt going and then the velocity and SOC are fine upto an extend because the plot of SOC is almost to my prediction but in velocities there are erroes like its not strating with zero which can be removed by using same mecahnism as beteen loops to mek it zero by np.insert like that and also the velocity dropedd in between and also thers a spike too the most erroe is by the api error in the code i tried but due to lack of time ifailed the assumption i took were a sunny day and didnt took stop between the drivers and took 3030 pomts with resolution of 100 m i previously treid with 1.5 km and the results were not that accurate but the plots were fine upto to an extend i treid to remove spikes from power but i reduced it from 8000 to 3000 w theres a heap like in SOC which inicates of it got charged might be at 12:00 time it might be the optimiser tried to minimize the energy and the time so it assued that the easy way would be to kkep velocity constant but it isnt possible practically 
